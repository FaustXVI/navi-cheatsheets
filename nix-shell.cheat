% nix, nix-shell

# test a package from a local nixpkgs clone
nix-shell -I nixpkgs=<pathToNixpkgs> -p <packages>

$ pathToNixpkgs: pwd

% nix, nix-shell

# run a command in a nix-shell
nix-shell --run '<command>'
